enableFeaturePreview('VERSION_CATALOGS')
dependencyResolutionManagement {
    defaultLibrariesExtensionName.set('deps')
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "bytecode"
include ':app'
